{
  "name": "build-time",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview",
    "deploy": "npm run build && cd deployments && vercel link --project bt_allesverkauf --yes && vercel --prod"
  }
}
